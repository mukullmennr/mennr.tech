export const metadata = {
    title: "Mennr Marketing Solutions for Small Businesses",
};

interface ClientLayoutProps {
    children: React.ReactNode;
}

export default function ClientLayout({ children }: ClientLayoutProps) {
    return children;
}
