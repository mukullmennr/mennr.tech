export const metadata = {
    title: "The Role of Meta Ads in Marketing For Small Businesses",
};

interface ClientLayoutProps {
    children: React.ReactNode;
}

export default function ClientLayout({ children }: ClientLayoutProps) {
    return children;
}